VITE_API_BASE_URL=http://localhost:8000/api
# The real backend now exists in server/ (verified working end-to-end —
# see server/README.md). Defaulting to mock here so `npm run dev` still
# works without MySQL/the API server running; flip to false to test
# against the real backend locally.
VITE_USE_MOCK_API=true
